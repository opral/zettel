---
"@opral/zettel-html": patch
---

Ignore document metadata (`<meta>`, `<link>`, `<base>`, `<title>`) when importing HTML, so pasting from a web page in Chromium no longer adds a read-only `<meta charset="utf-8">` block.
