---
"@opral/zettel-lexical": patch
---

Apply spell-check suggestions and text drops, whose text arrives in `dataTransfer` rather than `InputEvent.data`, and remove the source text of a drag-and-drop move.
