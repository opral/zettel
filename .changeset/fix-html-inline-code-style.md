---
"@opral/zettel-html": patch
---

Style inline code as a chip with `--zettel-code-background` and `--zettel-code-color`, the same in the editor and in rendered HTML. Before, inline code only switched to a monospace font, and only code blocks used the code colors. Code inside code blocks is not styled as a chip, and linked code keeps the link color.
