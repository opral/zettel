---
"@opral/zettel-lexical": patch
---

Keep text typed after a trailing Shift+Enter hard break. Hard breaks are now Lexical line breaks, so the caret can no longer land inside one, the new line is visible, and select-all deletion removes them.
