---
"@opral/zettel-html": patch
---

Import an inline element that wraps block content (as Google Docs does with `<b id="docs-internal-guid-…">`) as a container, so pasted paragraphs and lists stay editable instead of becoming read-only inline HTML.
